## Deprecated discovery site

The standalone registry site is deprecated. The canonical public browse UI now lives in the wizard service at `/browse`, where official templates and community submissions appear together.

Keep [`../index.json`](../index.json), the schema, and validation scripts as the community submission source of truth. Do not add new UI features here.

### Deploying

The included `render.yaml` remains only for compatibility with old standalone registry deploys.
