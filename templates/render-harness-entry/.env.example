# Required: model API key.
ANTHROPIC_API_KEY=

# Required: Postgres connection string. Locally, point at the harness
# compose stack: postgres://harness:harness@127.0.0.1:55432/harness
DATABASE_URL=

# Optional: Render API key, only if you wire the Render MCP.
# RENDER_API_KEY=
